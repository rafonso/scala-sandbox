package sandbox

import scala.collection.mutable.Buffer

object PrimesAsJava {
  
  def isPrime(i: Int, priorPrimes:  Iterable[Int]): Boolean = {
    var prime: Boolean = true
    
    
    
    prime
  }
  
  def main(args : Array[String]) : Unit = {}
}
